<h1>面接対策問題一覧</h1>
<% @questions.each do |q| %>
  <%= link_to q.content, question_path(q) %>
  <%= link_to "編集", edit_question_path(q) %>
  <%= link_to "削除", question_path(q), method: :delete, data: { confirm: "本当に削除していい?"} %>
<% end %>